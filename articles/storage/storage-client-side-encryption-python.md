---
title: "Šifrování na straně klienta s Python pro Microsoft Azure Storage | Microsoft Docs"
description: "Klientská knihovna pro úložiště Azure pro jazyk Python podporuje šifrování na straně klienta pro maximální zabezpečení pro vaše aplikace Azure Storage."
services: storage
documentationcenter: python
author: lakasa
manager: jahogg
editor: tysonn
ms.assetid: f9bf7981-9948-4f83-8931-b15679a09b8a
ms.service: storage
ms.workload: storage
ms.tgt_pltfrm: na
ms.devlang: python
ms.topic: article
ms.date: 05/11/2017
ms.author: lakasa
ms.openlocfilehash: 95330d2662722784beabdf51c9331fdeb502fc53
ms.sourcegitcommit: f537befafb079256fba0529ee554c034d73f36b0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/11/2017
---
# <a name="client-side-encryption-with-python-for-microsoft-azure-storage"></a><span data-ttu-id="072db-103">Šifrování na straně klienta s Python pro Microsoft Azure Storage</span><span class="sxs-lookup"><span data-stu-id="072db-103">Client-Side Encryption with Python for Microsoft Azure Storage</span></span>
[!INCLUDE [storage-selector-client-side-encryption-include](../../includes/storage-selector-client-side-encryption-include.md)]

## <a name="overview"></a><span data-ttu-id="072db-104">Přehled</span><span class="sxs-lookup"><span data-stu-id="072db-104">Overview</span></span>
<span data-ttu-id="072db-105">[Klientská knihovna pro úložiště Azure pro jazyk Python](https://pypi.python.org/pypi/azure-storage) podporuje šifrování dat v rámci klientské aplikace před nahráním do úložiště Azure a dešifrování dat při stahování do klienta.</span><span class="sxs-lookup"><span data-stu-id="072db-105">The [Azure Storage Client Library for Python](https://pypi.python.org/pypi/azure-storage) supports encrypting data within client applications before uploading to Azure Storage, and decrypting data while downloading to the client.</span></span>

> [!NOTE]
> <span data-ttu-id="072db-106">Knihovna Python úložiště Azure je ve verzi preview.</span><span class="sxs-lookup"><span data-stu-id="072db-106">The Azure Storage Python library is in preview.</span></span>
> 
> 

## <a name="encryption-and-decryption-via-the-envelope-technique"></a><span data-ttu-id="072db-107">Šifrování a dešifrování pomocí technik obálky</span><span class="sxs-lookup"><span data-stu-id="072db-107">Encryption and decryption via the envelope technique</span></span>
<span data-ttu-id="072db-108">Procesy šifrování a dešifrování podle technika obálku.</span><span class="sxs-lookup"><span data-stu-id="072db-108">The processes of encryption and decryption follow the envelope technique.</span></span>

### <a name="encryption-via-the-envelope-technique"></a><span data-ttu-id="072db-109">Šifrování pomocí technik obálky</span><span class="sxs-lookup"><span data-stu-id="072db-109">Encryption via the envelope technique</span></span>
<span data-ttu-id="072db-110">Šifrování pomocí technik obálky funguje následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="072db-110">Encryption via the envelope technique works in the following way:</span></span>

1. <span data-ttu-id="072db-111">Klientská knihovna pro úložiště Azure generuje obsahu šifrovací klíč (CEK), což je použití jednoho bránu symetrického klíče.</span><span class="sxs-lookup"><span data-stu-id="072db-111">The Azure storage client library generates a content encryption key (CEK), which is a one-time-use symmetric key.</span></span>
2. <span data-ttu-id="072db-112">Data se šifrují pomocí této CEK.</span><span class="sxs-lookup"><span data-stu-id="072db-112">User data is encrypted using this CEK.</span></span>
3. <span data-ttu-id="072db-113">CEK je vnořena (šifrované) pomocí klíčů šifrovacího klíče (KEK).</span><span class="sxs-lookup"><span data-stu-id="072db-113">The CEK is then wrapped (encrypted) using the key encryption key (KEK).</span></span> <span data-ttu-id="072db-114">Klíče KEK je identifikovaná identifikátorem klíče a může být pár asymetrických klíčů nebo symetrický klíč, který je spravovaný místně.</span><span class="sxs-lookup"><span data-stu-id="072db-114">The KEK is identified by a key identifier and can be an asymmetric key pair or a symmetric key, which is managed locally.</span></span>
   <span data-ttu-id="072db-115">Klientská knihovna pro úložiště, samotné nikdy má přístup k KEK.</span><span class="sxs-lookup"><span data-stu-id="072db-115">The storage client library itself never has access to KEK.</span></span> <span data-ttu-id="072db-116">Knihovny vyvolá algoritmus zabalení klíče, který zajišťuje klíče KEK.</span><span class="sxs-lookup"><span data-stu-id="072db-116">The library invokes the key wrapping algorithm that is provided by the KEK.</span></span> <span data-ttu-id="072db-117">Uživatelé mohou používat vlastní zprostředkovatele pro klíče zabalení/rozbalování v případě potřeby.</span><span class="sxs-lookup"><span data-stu-id="072db-117">Users can choose to use custom providers for key wrapping/unwrapping if desired.</span></span>
4. <span data-ttu-id="072db-118">Šifrovaná data se pak odešlou do služby Azure Storage.</span><span class="sxs-lookup"><span data-stu-id="072db-118">The encrypted data is then uploaded to the Azure Storage service.</span></span> <span data-ttu-id="072db-119">Zabalená klíč společně se některé další šifrování metadat je uložena jako metadata (na binární rozsáhlý objekt) nebo interpolované s šifrovaná data (zprávy fronty a entity tabulky).</span><span class="sxs-lookup"><span data-stu-id="072db-119">The wrapped key along with some additional encryption metadata is either stored as metadata (on a blob) or interpolated with the encrypted data (queue messages and table entities).</span></span>

### <a name="decryption-via-the-envelope-technique"></a><span data-ttu-id="072db-120">Dešifrování pomocí technik obálky</span><span class="sxs-lookup"><span data-stu-id="072db-120">Decryption via the envelope technique</span></span>
<span data-ttu-id="072db-121">Dešifrování pomocí technik obálky funguje následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="072db-121">Decryption via the envelope technique works in the following way:</span></span>

1. <span data-ttu-id="072db-122">Klientská knihovna předpokládá, že uživatel je Správa klíčů šifrovací klíč (KEK) místně.</span><span class="sxs-lookup"><span data-stu-id="072db-122">The client library assumes that the user is managing the key encryption key (KEK) locally.</span></span> <span data-ttu-id="072db-123">Uživatel nemusí vědět konkrétní klíč, který slouží k šifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-123">The user does not need to know the specific key that was used for encryption.</span></span> <span data-ttu-id="072db-124">Překladač klíče, který se přeloží různé klíče identifikátory ke klíčům, místo toho můžete nastavit a používat.</span><span class="sxs-lookup"><span data-stu-id="072db-124">Instead, a key resolver, which resolves different key identifiers to keys, can be set up and used.</span></span>
2. <span data-ttu-id="072db-125">Klientská knihovna stáhne šifrovaná data spolu se žádné šifrování materiál, který je uložený ve službě.</span><span class="sxs-lookup"><span data-stu-id="072db-125">The client library downloads the encrypted data along with any encryption material that is stored on the service.</span></span>
3. <span data-ttu-id="072db-126">Zabalená obsahu šifrovací klíč (CEK) je pak rozbalenou (dešifrovaný) používání klíčů šifrovací klíč (KEK).</span><span class="sxs-lookup"><span data-stu-id="072db-126">The wrapped content encryption key (CEK) is then unwrapped (decrypted) using the key encryption key (KEK).</span></span> <span data-ttu-id="072db-127">Zde znovu, knihovny klienta nemá přístup k KEK.</span><span class="sxs-lookup"><span data-stu-id="072db-127">Here again, the client library does not have access to KEK.</span></span> <span data-ttu-id="072db-128">Vyvolá jednoduše rozbalení algoritmem vlastního zprostředkovatele.</span><span class="sxs-lookup"><span data-stu-id="072db-128">It simply invokes the custom provider's unwrapping algorithm.</span></span>
4. <span data-ttu-id="072db-129">Obsahu šifrovací klíč (CEK) se pak používá k dešifrování dat šifrovaných uživatele.</span><span class="sxs-lookup"><span data-stu-id="072db-129">The content encryption key (CEK) is then used to decrypt the encrypted user data.</span></span>

## <a name="encryption-mechanism"></a><span data-ttu-id="072db-130">Mechanismus šifrování</span><span class="sxs-lookup"><span data-stu-id="072db-130">Encryption Mechanism</span></span>
<span data-ttu-id="072db-131">Klientská knihovna pro úložiště používá [AES](http://en.wikipedia.org/wiki/Advanced_Encryption_Standard) k šifrování dat uživatele.</span><span class="sxs-lookup"><span data-stu-id="072db-131">The storage client library uses [AES](http://en.wikipedia.org/wiki/Advanced_Encryption_Standard) in order to encrypt user data.</span></span> <span data-ttu-id="072db-132">Konkrétně [šifrovací bloku algoritmem CBC](http://en.wikipedia.org/wiki/Block_cipher_mode_of_operation#Cipher-block_chaining_.28CBC.29) režimu pomocí standardu AES.</span><span class="sxs-lookup"><span data-stu-id="072db-132">Specifically, [Cipher Block Chaining (CBC)](http://en.wikipedia.org/wiki/Block_cipher_mode_of_operation#Cipher-block_chaining_.28CBC.29) mode with AES.</span></span> <span data-ttu-id="072db-133">Každý trochu jinak, služba funguje, se budeme zabývat každý z nich zde.</span><span class="sxs-lookup"><span data-stu-id="072db-133">Each service works somewhat differently, so we will discuss each of them here.</span></span>

### <a name="blobs"></a><span data-ttu-id="072db-134">Objekty blob</span><span class="sxs-lookup"><span data-stu-id="072db-134">Blobs</span></span>
<span data-ttu-id="072db-135">Klientská knihovna aktuálně podporuje šifrování pouze celý objekty BLOB.</span><span class="sxs-lookup"><span data-stu-id="072db-135">The client library currently supports encryption of whole blobs only.</span></span> <span data-ttu-id="072db-136">Konkrétně je podporováno šifrování, pokud uživatelé používají **vytvořit*** metody.</span><span class="sxs-lookup"><span data-stu-id="072db-136">Specifically, encryption is supported when users use the **create*** methods.</span></span> <span data-ttu-id="072db-137">Pro stahování, obě dokončení a rozsah stahování jsou podporované a paralelizace nahrávání a stahování je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="072db-137">For downloads, both complete and range downloads are supported, and parallelization of both upload and download is available.</span></span>

<span data-ttu-id="072db-138">Během šifrování se klientské knihovny bude generovat náhodných inicializace vektoru (IV) 16 bajtů, společně s náhodných obsahu šifrovací klíč (CEK) 32 bajtů a provádí obálky šifrování dat objektů blob na základě těchto informací.</span><span class="sxs-lookup"><span data-stu-id="072db-138">During encryption, the client library will generate a random Initialization Vector (IV) of 16 bytes, together with a random content encryption key (CEK) of 32 bytes, and perform envelope encryption of the blob data using this information.</span></span> <span data-ttu-id="072db-139">Zabalená CEK a některé další šifrování metadata jsou pak uloženy jako objekt blob metadat spolu s zašifrovaný objekt blob ve službě.</span><span class="sxs-lookup"><span data-stu-id="072db-139">The wrapped CEK and some additional encryption metadata are then stored as blob metadata along with the encrypted blob on the service.</span></span>

> [!WARNING]
> <span data-ttu-id="072db-140">Pokud jsou úpravy nebo odesílání vlastní metadata pro objekt blob, je třeba zajistit, že se zachová, i tato metadata.</span><span class="sxs-lookup"><span data-stu-id="072db-140">If you are editing or uploading your own metadata for the blob, you need to ensure that this metadata is preserved.</span></span> <span data-ttu-id="072db-141">Pokud nahrajete novými metadaty bez těchto metadat, bude ztracena zabalené CEK, IV a další metadata a obsah objektu blob se nikdy bude nenávratně ztracený.</span><span class="sxs-lookup"><span data-stu-id="072db-141">If you upload new metadata without this metadata, the wrapped CEK, IV and other metadata will be lost and the blob content will never be retrievable again.</span></span>
> 
> 

<span data-ttu-id="072db-142">Stahování zašifrovaný objekt blob zahrnuje načítání obsah pomocí celý objekt blob **získat*** usnadňující metody.</span><span class="sxs-lookup"><span data-stu-id="072db-142">Downloading an encrypted blob involves retrieving the content of the entire blob using the **get*** convenience methods.</span></span> <span data-ttu-id="072db-143">Zabalená CEK je úkony, spočívající a společně s IV (uložené v tomto případě jako metadata objektu blob) používá k vrácení dešifrovaná data pro uživatele.</span><span class="sxs-lookup"><span data-stu-id="072db-143">The wrapped CEK is unwrapped and used together with the IV (stored as blob metadata in this case) to return the decrypted data to the users.</span></span>

<span data-ttu-id="072db-144">Stahování libovolný rozsah (**získat*** předané metody s parametry rozsahu) v zašifrovaný objekt blob zahrnuje úpravy rozsahu poskytované uživatelé mohli malé množství další data, která slouží k dešifrování úspěšně požadovaný rozsah.</span><span class="sxs-lookup"><span data-stu-id="072db-144">Downloading an arbitrary range (**get*** methods with range parameters passed in) in the encrypted blob involves adjusting the range provided by users in order to get a small amount of additional data that can be used to successfully decrypt the requested range.</span></span>

<span data-ttu-id="072db-145">Objekty BLOB bloku a objekty BLOB stránky může být pouze šifrovat nebo dešifrovat použití tohoto schématu.</span><span class="sxs-lookup"><span data-stu-id="072db-145">Block blobs and page blobs only can be encrypted/decrypted using this scheme.</span></span> <span data-ttu-id="072db-146">Aktuálně nepodporuje se pro šifrování doplňovací objekty BLOB.</span><span class="sxs-lookup"><span data-stu-id="072db-146">There is currently no support for encrypting append blobs.</span></span>

### <a name="queues"></a><span data-ttu-id="072db-147">Fronty</span><span class="sxs-lookup"><span data-stu-id="072db-147">Queues</span></span>
<span data-ttu-id="072db-148">Vzhledem k tomu, že fronta zprávy můžou být libovolném formátu, klientské knihovny definuje vlastní formát, který zahrnuje inicializační vektor (IV) a šifrované obsahu šifrovací klíč (CEK) v textu zprávy.</span><span class="sxs-lookup"><span data-stu-id="072db-148">Since queue messages can be of any format, the client library defines a custom format that includes the Initialization Vector (IV) and the encrypted content encryption key (CEK) in the message text.</span></span>

<span data-ttu-id="072db-149">Během šifrování se klientské knihovny generuje náhodné IV 16 bajtů společně s náhodných CEK 32 bajtů a provádí šifrování obálky text zprávy fronty pomocí těchto informací.</span><span class="sxs-lookup"><span data-stu-id="072db-149">During encryption, the client library generates a random IV of 16 bytes along with a random CEK of 32 bytes and performs envelope encryption of the queue message text using this information.</span></span> <span data-ttu-id="072db-150">Zabalená CEK a některé další šifrování metadat se pak přidají do zprávy ve frontě šifrované.</span><span class="sxs-lookup"><span data-stu-id="072db-150">The wrapped CEK and some additional encryption metadata are then added to the encrypted queue message.</span></span> <span data-ttu-id="072db-151">Této upravené zprávy (zobrazené dole) je uložený ve službě.</span><span class="sxs-lookup"><span data-stu-id="072db-151">This modified message (shown below) is stored on the service.</span></span>

```
<MessageText>{"EncryptedMessageContents":"6kOu8Rq1C3+M1QO4alKLmWthWXSmHV3mEfxBAgP9QGTU++MKn2uPq3t2UjF1DO6w","EncryptionData":{…}}</MessageText>
```

<span data-ttu-id="072db-152">Během dešifrování je zabalené klíč extrahovaným ze zprávy ve frontě a úkony, spočívající.</span><span class="sxs-lookup"><span data-stu-id="072db-152">During decryption, the wrapped key is extracted from the queue message and unwrapped.</span></span> <span data-ttu-id="072db-153">IV je také extrahovaným ze zprávy ve frontě a používat společně s rozbalenou klíčem k dešifrování dat zprávu fronty.</span><span class="sxs-lookup"><span data-stu-id="072db-153">The IV is also extracted from the queue message and used along with the unwrapped key to decrypt the queue message data.</span></span> <span data-ttu-id="072db-154">Všimněte si, že metadata šifrování je malá (v bajtech) 500, takže když ho započítávat limit 64KB pro zprávu fronty, by mělo být dopad spravovat.</span><span class="sxs-lookup"><span data-stu-id="072db-154">Note that the encryption metadata is small (under 500 bytes), so while it does count toward the 64KB limit for a queue message, the impact should be manageable.</span></span>

### <a name="tables"></a><span data-ttu-id="072db-155">Tabulky</span><span class="sxs-lookup"><span data-stu-id="072db-155">Tables</span></span>
<span data-ttu-id="072db-156">Klientská knihovna podporuje šifrování vlastností entity pro vložení a nahrazovat operace.</span><span class="sxs-lookup"><span data-stu-id="072db-156">The client library supports encryption of entity properties for insert and replace operations.</span></span>

> [!NOTE]
> <span data-ttu-id="072db-157">Sloučení se aktuálně nepodporuje.</span><span class="sxs-lookup"><span data-stu-id="072db-157">Merge is not currently supported.</span></span> <span data-ttu-id="072db-158">Vzhledem k tomu, že podmnožinu vlastností může být šifrována dříve pomocí jiného klíče, jednoduše slučování nové vlastnosti a aktualizace metadat dojde ke ztrátě dat.</span><span class="sxs-lookup"><span data-stu-id="072db-158">Since a subset of properties may have been encrypted previously using a different key, simply merging the new properties and updating the metadata will result in data loss.</span></span> <span data-ttu-id="072db-159">Slučování buď vyžaduje volání další služby ke čtení existující entity ze služby, nebo pomocí nového klíče na vlastnosti, které nejsou vhodné z důvodů výkonu.</span><span class="sxs-lookup"><span data-stu-id="072db-159">Merging either requires making extra service calls to read the pre-existing entity from the service, or using a new key per property, both of which are not suitable for performance reasons.</span></span>
> 
> 

<span data-ttu-id="072db-160">Šifrování dat tabulky funguje takto:</span><span class="sxs-lookup"><span data-stu-id="072db-160">Table data encryption works as follows:</span></span>

1. <span data-ttu-id="072db-161">Uživatelé zadat vlastnosti k zašifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-161">Users specify the properties to be encrypted.</span></span>
2. <span data-ttu-id="072db-162">Klientská knihovna generuje náhodných inicializace vektoru (IV) 16 bajtů společně s klíčem náhodných šifrování obsahu (CEK) 32 bajtů pro každou entitu a provádí šifrování obálky na jednotlivé vlastnosti k zašifrování odvozením nová IV za vlastnost.</span><span class="sxs-lookup"><span data-stu-id="072db-162">The client library generates a random Initialization Vector (IV) of 16 bytes along with a random content encryption key (CEK) of 32 bytes for every entity, and performs envelope encryption on the individual properties to be encrypted by deriving a new IV per property.</span></span> <span data-ttu-id="072db-163">Zašifrované vlastnosti se ukládají jako binární data.</span><span class="sxs-lookup"><span data-stu-id="072db-163">The encrypted property is stored as binary data.</span></span>
3. <span data-ttu-id="072db-164">Zabalená CEK a některé další šifrování metadata jsou pak uloženy jako dva další rezervované vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="072db-164">The wrapped CEK and some additional encryption metadata are then stored as two additional reserved properties.</span></span> <span data-ttu-id="072db-165">První rezervované vlastnosti (\_ClientEncryptionMetadata1) se ve vlastnosti string, který obsahuje informace o IV, verzi a zabalené klíč.</span><span class="sxs-lookup"><span data-stu-id="072db-165">The first reserved property (\_ClientEncryptionMetadata1) is a string property that holds the information about IV, version, and wrapped key.</span></span> <span data-ttu-id="072db-166">Druhý rezervované vlastnosti (\_ClientEncryptionMetadata2) je binární vlastnost, která obsahuje informace o vlastnosti, které jsou zašifrované.</span><span class="sxs-lookup"><span data-stu-id="072db-166">The second reserved property (\_ClientEncryptionMetadata2) is a binary property that holds the information about the properties that are encrypted.</span></span> <span data-ttu-id="072db-167">Informace v této druhé vlastnosti (\_ClientEncryptionMetadata2) je sám zašifrovaná.</span><span class="sxs-lookup"><span data-stu-id="072db-167">The information in this second property (\_ClientEncryptionMetadata2) is itself encrypted.</span></span>
4. <span data-ttu-id="072db-168">Z důvodu tyto další rezervované vlastnosti vyžadované pro šifrování uživatelé nyní mohou mít pouze 250 vlastní vlastnosti místo 252.</span><span class="sxs-lookup"><span data-stu-id="072db-168">Due to these additional reserved properties required for encryption, users may now have only 250 custom properties instead of 252.</span></span> <span data-ttu-id="072db-169">Celková velikost entity, musí být menší než 1MB.</span><span class="sxs-lookup"><span data-stu-id="072db-169">The total size of the entity must be less than 1MB.</span></span>
   
   <span data-ttu-id="072db-170">Všimněte si, že pouze vlastnosti řetězce mohou být šifrována.</span><span class="sxs-lookup"><span data-stu-id="072db-170">Note that only string properties can be encrypted.</span></span> <span data-ttu-id="072db-171">Pokud jsou i další typy vlastností k šifrování, musí být převeden na řetězce.</span><span class="sxs-lookup"><span data-stu-id="072db-171">If other types of properties are to be encrypted, they must be converted to strings.</span></span> <span data-ttu-id="072db-172">Šifrované řetězce jsou uložené ve službě jako binární vlastnosti a převedené zpět do řetězce (nezpracovaná řetězce, není EntityProperties s typem EdmType.STRING) po dešifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-172">The encrypted strings are stored on the service as binary properties, and they are converted back to strings (raw strings, not EntityProperties with type EdmType.STRING) after decryption.</span></span>
   
   <span data-ttu-id="072db-173">Pro tabulky, kromě zásady šifrování musí uživatelé zadat vlastnosti k zašifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-173">For tables, in addition to the encryption policy, users must specify the properties to be encrypted.</span></span> <span data-ttu-id="072db-174">To lze provést buď ukládání těchto vlastností v objektech TableEntity je typ nastaven na EdmType.STRING a šifrování nastaven na hodnotu true nebo nastavením encryption_resolver_function tableservice objektu.</span><span class="sxs-lookup"><span data-stu-id="072db-174">This can be done by either storing these properties in TableEntity objects with the type set to EdmType.STRING and encrypt set to true or setting the encryption_resolver_function on the tableservice object.</span></span> <span data-ttu-id="072db-175">Překladač šifrování je funkce, která přebírá klíč oddílu, klíč řádku a název vlastnosti a vrátí logickou hodnotu, která určuje, jestli by se šifrovat tuto vlastnost.</span><span class="sxs-lookup"><span data-stu-id="072db-175">An encryption resolver is a function that takes a partition key, row key, and property name and returns a boolean that indicates whether that property should be encrypted.</span></span> <span data-ttu-id="072db-176">Během šifrování se klientské knihovny použije tyto informace se rozhodnout, jestli by se vlastnost šifrovat při zápisu do sítě.</span><span class="sxs-lookup"><span data-stu-id="072db-176">During encryption, the client library will use this information to decide whether a property should be encrypted while writing to the wire.</span></span> <span data-ttu-id="072db-177">Delegát taky poskytuje možnost logiku kolem jak jsou zašifrované vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="072db-177">The delegate also provides for the possibility of logic around how properties are encrypted.</span></span> <span data-ttu-id="072db-178">(Například pokud X, potom šifrování vlastnost A; v opačném případě šifrování vlastnosti A a B.) Všimněte si, že není nutné poskytnout tyto informace při čtení nebo dotazování entity.</span><span class="sxs-lookup"><span data-stu-id="072db-178">(For example, if X, then encrypt property A; otherwise encrypt properties A and B.) Note that it is not necessary to provide this information while reading or querying entities.</span></span>

### <a name="batch-operations"></a><span data-ttu-id="072db-179">Dávkové operace</span><span class="sxs-lookup"><span data-stu-id="072db-179">Batch Operations</span></span>
<span data-ttu-id="072db-180">Jedna zásada šifrování se vztahuje na všechny řádky v dávce.</span><span class="sxs-lookup"><span data-stu-id="072db-180">One encryption policy applies to all rows in the batch.</span></span> <span data-ttu-id="072db-181">Klientská knihovna interně vygenerujte nový náhodný IV a náhodných CEK na řádek v dávce.</span><span class="sxs-lookup"><span data-stu-id="072db-181">The client library will internally generate a new random IV and random CEK per row in the batch.</span></span> <span data-ttu-id="072db-182">Uživatelé mohou také k šifrování různé vlastnosti pro všechny operace v dávce definováním toto chování v překladač šifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-182">Users can also choose to encrypt different properties for every operation in the batch by defining this behavior in the encryption resolver.</span></span>
<span data-ttu-id="072db-183">Pokud dávky je vytvořen jako správce kontextu prostřednictvím metody batch() tableservice, zásady šifrování tableservice bude automaticky použita pro dávku.</span><span class="sxs-lookup"><span data-stu-id="072db-183">If a batch is created as a context manager through the tableservice batch() method, the tableservice's encryption policy will automatically be applied to the batch.</span></span> <span data-ttu-id="072db-184">Dávky explicitně vytvořena při volání konstruktoru, musí být předán jako parametr a vlevo ponechat beze změny po dobu jeho existence dávky zásady šifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-184">If a batch is created explicitly by calling the constructor, the encryption policy must be passed as a parameter and left unmodified for the lifetime of the batch.</span></span>
<span data-ttu-id="072db-185">Všimněte si, že entit je jím zašifrovaná, jako jsou vloženy do dávky pomocí zásad šifrování batch (entit je jím zašifrovaná není v době potvrzení batch pomocí zásad šifrování tableservice).</span><span class="sxs-lookup"><span data-stu-id="072db-185">Note that entities are encrypted as they are inserted into the batch using the batch's encryption policy (entities are NOT encrypted at the time of committing the batch using the tableservice's encryption policy).</span></span>

### <a name="queries"></a><span data-ttu-id="072db-186">Dotazy</span><span class="sxs-lookup"><span data-stu-id="072db-186">Queries</span></span>
<span data-ttu-id="072db-187">K provedení operace dotazů, je nutné zadat klíče překladače, který se bude moct vyřešit všechny klíče v sadě výsledků dotazu.</span><span class="sxs-lookup"><span data-stu-id="072db-187">To perform query operations, you must specify a key resolver that is able to resolve all the keys in the result set.</span></span> <span data-ttu-id="072db-188">Pokud entity obsažené ve výsledku dotazu nelze přeložit na poskytovatele, knihovny klienta vyvolá chybu.</span><span class="sxs-lookup"><span data-stu-id="072db-188">If an entity contained in the query result cannot be resolved to a provider, the client library will throw an error.</span></span> <span data-ttu-id="072db-189">Po jakémkoli dotazu, který provádí projekce straně serveru, budou klientské knihovny přidat metadata vlastnosti speciální šifrování (\_ClientEncryptionMetadata1 a \_ClientEncryptionMetadata2) ve výchozím nastavení vybrané sloupce.</span><span class="sxs-lookup"><span data-stu-id="072db-189">For any query that performs server side projections, the client library will add the special encryption metadata properties (\_ClientEncryptionMetadata1 and \_ClientEncryptionMetadata2) by default to the selected columns.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="072db-190">Mějte na paměti tyto důležité bodů při použití šifrování na straně klienta:</span><span class="sxs-lookup"><span data-stu-id="072db-190">Be aware of these important points when using client-side encryption:</span></span>
> 
> * <span data-ttu-id="072db-191">Při čtení nebo zápisu do zašifrovaný objekt blob, pomocí příkazů nahrávání celý objekt blob a rozsah nebo celé blob stažení příkazy.</span><span class="sxs-lookup"><span data-stu-id="072db-191">When reading from or writing to an encrypted blob, use whole blob upload commands and range/whole blob download commands.</span></span> <span data-ttu-id="072db-192">Vyhněte se zápis do zašifrovaný objekt blob pomocí protokolu operací, jako je Put bloku uvést seznam blokovaných, zápis stránky stránkách nebo zrušte; jinak může dojít k poškození zašifrovaný objekt blob a nastavit jej jako nečitelná.</span><span class="sxs-lookup"><span data-stu-id="072db-192">Avoid writing to an encrypted blob using protocol operations such as Put Block, Put Block List, Write Pages, or Clear Pages; otherwise you may corrupt the encrypted blob and make it unreadable.</span></span>
> * <span data-ttu-id="072db-193">Pro tabulky podobně jako omezení existuje.</span><span class="sxs-lookup"><span data-stu-id="072db-193">For tables, a similar constraint exists.</span></span> <span data-ttu-id="072db-194">Pečlivě nelze aktualizovat zašifrované vlastnosti bez aktualizace metadata šifrování.</span><span class="sxs-lookup"><span data-stu-id="072db-194">Be careful to not update encrypted properties without updating the encryption metadata.</span></span>
> * <span data-ttu-id="072db-195">Pokud jste nastavili metadata na zašifrovaný objekt blob, může přepsat metadata šifrování požadované pro dešifrování, protože není sčítání nastavení metadat.</span><span class="sxs-lookup"><span data-stu-id="072db-195">If you set metadata on the encrypted blob, you may overwrite the encryption-related metadata required for decryption, since setting metadata is not additive.</span></span> <span data-ttu-id="072db-196">To platí také pro snímky; Vyhněte se zadání metadat při vytváření snímku zašifrovaný objekt blob.</span><span class="sxs-lookup"><span data-stu-id="072db-196">This is also true for snapshots; avoid specifying metadata while creating a snapshot of an encrypted blob.</span></span> <span data-ttu-id="072db-197">Pokud musí být nastavená metadata, je nutné volat **get_blob_metadata** metodu nejdřív získat aktuální metadata šifrování a zabránit souběžných zápisy při nastavování metadat.</span><span class="sxs-lookup"><span data-stu-id="072db-197">If metadata must be set, be sure to call the **get_blob_metadata** method first to get the current encryption metadata, and avoid concurrent writes while metadata is being set.</span></span>
> * <span data-ttu-id="072db-198">Povolit **require_encryption** příznak na objekt služby pro uživatele, kteří by měla fungovat jenom s šifrovaná data.</span><span class="sxs-lookup"><span data-stu-id="072db-198">Enable the **require_encryption** flag on the service object for users that should work only with encrypted data.</span></span> <span data-ttu-id="072db-199">Další informace najdete níže.</span><span class="sxs-lookup"><span data-stu-id="072db-199">See below for more info.</span></span>
> 
> 

<span data-ttu-id="072db-200">Klientská knihovna pro úložiště očekává, že zadaný KEK a klíče překladač implementovat následující rozhraní.</span><span class="sxs-lookup"><span data-stu-id="072db-200">The storage client library expects the provided KEK and key resolver to implement the following interface.</span></span> <span data-ttu-id="072db-201">[Azure Key Vault](https://azure.microsoft.com/services/key-vault/) podporu pro správu Python KEK čeká na vyřízení a integrují do této knihovny po dokončení.</span><span class="sxs-lookup"><span data-stu-id="072db-201">[Azure Key Vault](https://azure.microsoft.com/services/key-vault/) support for Python KEK management is pending and will be integrated into this library when completed.</span></span>

## <a name="client-api--interface"></a><span data-ttu-id="072db-202">Klientské rozhraní API / rozhraní</span><span class="sxs-lookup"><span data-stu-id="072db-202">Client API / Interface</span></span>
<span data-ttu-id="072db-203">Po vytvoření objektu služby úložiště (tj. blockblobservice), uživatel může přiřadit hodnoty na pole, které tvoří zásady šifrování: key_encryption_key, key_resolver_function a require_encryption.</span><span class="sxs-lookup"><span data-stu-id="072db-203">After a storage service object (i.e. blockblobservice) has been created, the user may assign values to the fields that constitute an encryption policy: key_encryption_key, key_resolver_function, and require_encryption.</span></span> <span data-ttu-id="072db-204">Uživatelé zadat pouze KEK pouze překladač nebo obojí.</span><span class="sxs-lookup"><span data-stu-id="072db-204">Users can provide only a KEK, only a resolver, or both.</span></span> <span data-ttu-id="072db-205">key_encryption_key je základní typ klíče, který je identifikován pomocí identifikátoru klíče, který poskytuje logiku pro zabalení/rozbalování.</span><span class="sxs-lookup"><span data-stu-id="072db-205">key_encryption_key is the basic key type that is identified using a key identifier and that provides the logic for wrapping/unwrapping.</span></span> <span data-ttu-id="072db-206">key_resolver_function se používá k překladu během procesu dešifrovací klíč.</span><span class="sxs-lookup"><span data-stu-id="072db-206">key_resolver_function is used to resolve a key during the decryption process.</span></span> <span data-ttu-id="072db-207">Vrátí platný KEK zadaný identifikátor klíče.</span><span class="sxs-lookup"><span data-stu-id="072db-207">It returns a valid KEK given a key identifier.</span></span> <span data-ttu-id="072db-208">To poskytuje uživatelům možnost zvolit mezi více klíčů, které jsou spravovány v několika umístěních.</span><span class="sxs-lookup"><span data-stu-id="072db-208">This provides users the ability to choose between multiple keys that are managed in multiple locations.</span></span>

<span data-ttu-id="072db-209">Klíče KEK musí implementovat následující metody, které úspěšně šifrování dat:</span><span class="sxs-lookup"><span data-stu-id="072db-209">The KEK must implement the following methods to successfully encrypt data:</span></span>

* <span data-ttu-id="072db-210">wrap_key(cek): zabalí zadanou CEK (bajty) pomocí algoritmu podle volby uživatele.</span><span class="sxs-lookup"><span data-stu-id="072db-210">wrap_key(cek): Wraps the specified CEK (bytes) using an algorithm of the user's choice.</span></span> <span data-ttu-id="072db-211">Vrátí zabalené klíč.</span><span class="sxs-lookup"><span data-stu-id="072db-211">Returns the wrapped key.</span></span>
* <span data-ttu-id="072db-212">get_key_wrap_algorithm(): vrátí algoritmus slouží k zabalení klíče.</span><span class="sxs-lookup"><span data-stu-id="072db-212">get_key_wrap_algorithm(): Returns the algorithm used to wrap keys.</span></span>
* <span data-ttu-id="072db-213">get_kid(): vrátí řetězec id klíče pro tento KEK.</span><span class="sxs-lookup"><span data-stu-id="072db-213">get_kid(): Returns the string key id for this KEK.</span></span>
  <span data-ttu-id="072db-214">Klíče KEK musí implementovat následující metody, které úspěšně dešifrovat data:</span><span class="sxs-lookup"><span data-stu-id="072db-214">The KEK must implement the following methods to successfully decrypt data:</span></span>
* <span data-ttu-id="072db-215">unwrap_key (cek, algoritmus): vrátí rozbalenou formu zadaný CEK pomocí algoritmu zadaný řetězec.</span><span class="sxs-lookup"><span data-stu-id="072db-215">unwrap_key(cek, algorithm): Returns the unwrapped form of the specified CEK using the string-specified algorithm.</span></span>
* <span data-ttu-id="072db-216">get_kid(): vrací řetězec id klíče pro tento KEK.</span><span class="sxs-lookup"><span data-stu-id="072db-216">get_kid(): Returns a string key id for this KEK.</span></span>

<span data-ttu-id="072db-217">Překladač klíče alespoň musí implementovat metodu, která zadané id klíče, vrátí odpovídající KEK implementace rozhraní výše.</span><span class="sxs-lookup"><span data-stu-id="072db-217">The key resolver must at least implement a method that, given a key id, returns the corresponding KEK implementing the interface above.</span></span> <span data-ttu-id="072db-218">Jenom tato metoda je přiřazen key_resolver_function vlastnost v objektu služby.</span><span class="sxs-lookup"><span data-stu-id="072db-218">Only this method is to be assigned to the key_resolver_function property on the service object.</span></span>

* <span data-ttu-id="072db-219">Pro šifrování používá se vždy a chybí klíč bude mít za následek chybu.</span><span class="sxs-lookup"><span data-stu-id="072db-219">For encryption, the key is used always and the absence of a key will result in an error.</span></span>
* <span data-ttu-id="072db-220">K dešifrování:</span><span class="sxs-lookup"><span data-stu-id="072db-220">For decryption:</span></span>
  
  * <span data-ttu-id="072db-221">Překladač klíče je volána, pokud zadaný k získání klíče.</span><span class="sxs-lookup"><span data-stu-id="072db-221">The key resolver is invoked if specified to get the key.</span></span> <span data-ttu-id="072db-222">Pokud je zadaný překladač, ale nemá mapování pro identifikátor klíče, je vyvolána k chybě.</span><span class="sxs-lookup"><span data-stu-id="072db-222">If the resolver is specified but does not have a mapping for the key identifier, an error is thrown.</span></span>
  * <span data-ttu-id="072db-223">Pokud není zadaný překladač, ale je určen klíč, klíč se používá, pokud jeho identifikátoru odpovídá identifikátoru požadované klíče.</span><span class="sxs-lookup"><span data-stu-id="072db-223">If resolver is not specified but a key is specified, the key is used if its identifier matches the required key identifier.</span></span> <span data-ttu-id="072db-224">Pokud identifikátor neodpovídá, je vyvolána k chybě.</span><span class="sxs-lookup"><span data-stu-id="072db-224">If the identifier does not match, an error is thrown.</span></span>
    
    <span data-ttu-id="072db-225">Ukázky šifrování v azure.storage.samples <fix URL>předvádí podrobnější scénář začátku do konce pro objekty BLOB, fronty a tabulky.</span><span class="sxs-lookup"><span data-stu-id="072db-225">The encryption samples in azure.storage.samples <fix URL>demonstrate a more detailed end-to-end scenario for blobs, queues and tables.</span></span>
      <span data-ttu-id="072db-226">Ukázka implementace KEK a klíče překladač jsou uvedeny v ukázkové soubory jako KeyWrapper a KeyResolver v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="072db-226">Sample implementations of the KEK and key resolver are provided in the sample files as KeyWrapper and KeyResolver respectively.</span></span>

### <a name="requireencryption-mode"></a><span data-ttu-id="072db-227">Režim RequireEncryption</span><span class="sxs-lookup"><span data-stu-id="072db-227">RequireEncryption mode</span></span>
<span data-ttu-id="072db-228">Uživatelé mohou volitelně povolit režim operace, kde musí být všechny nahrávání a stahování zašifrována.</span><span class="sxs-lookup"><span data-stu-id="072db-228">Users can optionally enable a mode of operation where all uploads and downloads must be encrypted.</span></span> <span data-ttu-id="072db-229">V tomto režimu se nezdaří pokusy o odeslání dat bez zásady šifrování nebo data, která nejsou šifrována ve službě Stažení na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="072db-229">In this mode, attempts to upload data without an encryption policy or download data that is not encrypted on the service will fail on the client.</span></span> <span data-ttu-id="072db-230">**Require_encryption** příznak objektu Služba řídí toto chování.</span><span class="sxs-lookup"><span data-stu-id="072db-230">The **require_encryption** flag on the service object controls this behavior.</span></span>

### <a name="blob-service-encryption"></a><span data-ttu-id="072db-231">Šifrování služby objektů BLOB</span><span class="sxs-lookup"><span data-stu-id="072db-231">Blob service encryption</span></span>
<span data-ttu-id="072db-232">Nastavte šifrování polí zásad u objektu blockblobservice.</span><span class="sxs-lookup"><span data-stu-id="072db-232">Set the encryption policy fields on the blockblobservice object.</span></span> <span data-ttu-id="072db-233">Všem ostatním bude zpracovávat klientské knihovny interně.</span><span class="sxs-lookup"><span data-stu-id="072db-233">Everything else will be handled by the client library internally.</span></span>

```python
# Create the KEK used for encryption.
# KeyWrapper is the provided sample implementation, but the user may use their own object as long as it implements the interface above.
kek = KeyWrapper('local:key1') # Key identifier

# Create the key resolver used for decryption.
# KeyResolver is the provided sample implementation, but the user may use whatever implementation they choose so long as the function set on the service object behaves appropriately.
key_resolver = KeyResolver()
key_resolver.put_key(kek)

# Set the KEK and key resolver on the service object.
my_block_blob_service.key_encryption_key = kek
my_block_blob_service.key_resolver_funcion = key_resolver.resolve_key

# Upload the encrypted contents to the blob.
my_block_blob_service.create_blob_from_stream(container_name, blob_name, stream)

# Download and decrypt the encrypted contents from the blob.
blob = my_block_blob_service.get_blob_to_bytes(container_name, blob_name)
```

### <a name="queue-service-encryption"></a><span data-ttu-id="072db-234">Šifrování služby fronty</span><span class="sxs-lookup"><span data-stu-id="072db-234">Queue service encryption</span></span>
<span data-ttu-id="072db-235">Nastavte šifrování polí zásad u queueservice objektu.</span><span class="sxs-lookup"><span data-stu-id="072db-235">Set the encryption policy fields on the queueservice object.</span></span> <span data-ttu-id="072db-236">Všem ostatním bude zpracovávat klientské knihovny interně.</span><span class="sxs-lookup"><span data-stu-id="072db-236">Everything else will be handled by the client library internally.</span></span>

```python
# Create the KEK used for encryption.
# KeyWrapper is the provided sample implementation, but the user may use their own object as long as it implements the interface above.
kek = KeyWrapper('local:key1') # Key identifier

# Create the key resolver used for decryption.
# KeyResolver is the provided sample implementation, but the user may use whatever implementation they choose so long as the function set on the service object behaves appropriately.
key_resolver = KeyResolver()
key_resolver.put_key(kek)

# Set the KEK and key resolver on the service object.
my_queue_service.key_encryption_key = kek
my_queue_service.key_resolver_funcion = key_resolver.resolve_key

# Add message
my_queue_service.put_message(queue_name, content)

# Retrieve message
retrieved_message_list = my_queue_service.get_messages(queue_name)
```

### <a name="table-service-encryption"></a><span data-ttu-id="072db-237">Šifrování služby Table</span><span class="sxs-lookup"><span data-stu-id="072db-237">Table service encryption</span></span>
<span data-ttu-id="072db-238">Kromě vytváření zásad šifrování a jeho nastavení na žádost o možnostech, musíte buď určit **encryption_resolver_function** na **tableservice**, nebo nastavte atribut šifrování EntityProperty.</span><span class="sxs-lookup"><span data-stu-id="072db-238">In addition to creating an encryption policy and setting it on request options, you must either specify an **encryption_resolver_function** on the **tableservice**, or set the encrypt attribute on the EntityProperty.</span></span>

### <a name="using-the-resolver"></a><span data-ttu-id="072db-239">Pomocí překladače</span><span class="sxs-lookup"><span data-stu-id="072db-239">Using the resolver</span></span>

```python
# Create the KEK used for encryption.
# KeyWrapper is the provided sample implementation, but the user may use their own object as long as it implements the interface above.
kek = KeyWrapper('local:key1') # Key identifier

# Create the key resolver used for decryption.
# KeyResolver is the provided sample implementation, but the user may use whatever implementation they choose so long as the function set on the service object behaves appropriately.
key_resolver = KeyResolver()
key_resolver.put_key(kek)

# Define the encryption resolver_function.
def my_encryption_resolver(pk, rk, property_name):
        if property_name == 'foo':
                return True
        return False

# Set the KEK and key resolver on the service object.
my_table_service.key_encryption_key = kek
my_table_service.key_resolver_funcion = key_resolver.resolve_key
my_table_service.encryption_resolver_function = my_encryption_resolver

# Insert Entity
my_table_service.insert_entity(table_name, entity)

# Retrieve Entity
# Note: No need to specify an encryption resolver for retrieve, but it is harmless to leave the property set.
my_table_service.get_entity(table_name, entity['PartitionKey'], entity['RowKey'])
```

### <a name="using-attributes"></a><span data-ttu-id="072db-240">Pomocí atributů</span><span class="sxs-lookup"><span data-stu-id="072db-240">Using attributes</span></span>
<span data-ttu-id="072db-241">Jak je uvedeno nahoře, mohou být vlastnost označeny pro šifrování ukládání v objektu EntityProperty a nastavení šifrování pole.</span><span class="sxs-lookup"><span data-stu-id="072db-241">As mentioned above, a property may be marked for encryption by storing it in an EntityProperty object and setting the encrypt field.</span></span>

```python
encrypted_property_1 = EntityProperty(EdmType.STRING, value, encrypt=True)
```

## <a name="encryption-and-performance"></a><span data-ttu-id="072db-242">Šifrování a výkonu</span><span class="sxs-lookup"><span data-stu-id="072db-242">Encryption and performance</span></span>
<span data-ttu-id="072db-243">Všimněte si, že šifrování dat výsledky úložiště v dalších zatížení.</span><span class="sxs-lookup"><span data-stu-id="072db-243">Note that encrypting your storage data results in additional performance overhead.</span></span> <span data-ttu-id="072db-244">Musí být generovány klíč obsahu a IV, musí být šifrovaný samotný obsah a další metadata musí být naformátovaná a nahrát.</span><span class="sxs-lookup"><span data-stu-id="072db-244">The content key and IV must be generated, the content itself must be encrypted, and additional metadata must be formatted and uploaded.</span></span> <span data-ttu-id="072db-245">Tato dodatečná režie se bude lišit v závislosti na objemu dat šifrovaný.</span><span class="sxs-lookup"><span data-stu-id="072db-245">This overhead will vary depending on the quantity of data being encrypted.</span></span> <span data-ttu-id="072db-246">Doporučujeme vám, že zákazníci vždy testování aplikací pro výkon při vývoji.</span><span class="sxs-lookup"><span data-stu-id="072db-246">We recommend that customers always test their applications for performance during development.</span></span>

## <a name="next-steps"></a><span data-ttu-id="072db-247">Další kroky</span><span class="sxs-lookup"><span data-stu-id="072db-247">Next steps</span></span>
* <span data-ttu-id="072db-248">Stažení [Klientská knihovna pro úložiště Azure pro úložiště PyPi Java balíček](https://pypi.python.org/pypi/azure-storage)</span><span class="sxs-lookup"><span data-stu-id="072db-248">Download the [Azure Storage Client Library for Java PyPi package](https://pypi.python.org/pypi/azure-storage)</span></span>
* <span data-ttu-id="072db-249">Stažení [Klientská knihovna pro úložiště Azure pro jazyk Python zdrojový kód z Githubu](https://github.com/Azure/azure-storage-python)</span><span class="sxs-lookup"><span data-stu-id="072db-249">Download the [Azure Storage Client Library for Python Source Code from GitHub](https://github.com/Azure/azure-storage-python)</span></span>