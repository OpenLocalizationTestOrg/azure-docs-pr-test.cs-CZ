---
title: "Seznámení s OAuth2 implicitní tok poskytování ve službě Azure AD | Microsoft Docs"
description: "Další informace o službě Azure Active Directory provádění OAuth2 implicitní tok, poskytování a jestli je pro vaši aplikaci nejvhodnější."
services: active-directory
documentationcenter: dev-center-name
author: jmprieur
manager: mbaldwin
editor: 
ms.assetid: 90e42ff9-43b0-4b4f-a222-51df847b2a8d
ms.service: active-directory
ms.devlang: na
ms.topic: article
ms.tgt_pltfrm: na
ms.workload: identity
ms.date: 11/15/2016
ms.author: jmprieur
ms.custom: aaddev
ms.openlocfilehash: 241c744737515ee0c8d5d833a51121808877e559
ms.sourcegitcommit: f537befafb079256fba0529ee554c034d73f36b0
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/11/2017
---
# <a name="understanding-the-oauth2-implicit-grant-flow-in-azure-active-directory-ad"></a><span data-ttu-id="ee01e-103">Princip toku implicitní grant OAuth2 v Azure Active Directory (AD)</span><span class="sxs-lookup"><span data-stu-id="ee01e-103">Understanding the OAuth2 implicit grant flow in Azure Active Directory (AD)</span></span>
<span data-ttu-id="ee01e-104">Implicitní grant OAuth2 je notorious pro probíhá grant s nejdelší seznam zabezpečení se specifikací OAuth2.</span><span class="sxs-lookup"><span data-stu-id="ee01e-104">The OAuth2 implicit grant is notorious for being the grant with the longest list of security concerns in the OAuth2 specification.</span></span> <span data-ttu-id="ee01e-105">A ještě, který je implementované ADAL JS a ten, který doporučujeme při psaní aplikací SPA přístup.</span><span class="sxs-lookup"><span data-stu-id="ee01e-105">And yet, that is the approach implemented by ADAL JS and the one we recommend when writing SPA applications.</span></span> <span data-ttu-id="ee01e-106">Co nabízí?</span><span class="sxs-lookup"><span data-stu-id="ee01e-106">What gives?</span></span> <span data-ttu-id="ee01e-107">Je všechny řádu kompromisy: jak se jím implicitní grant se je nejlepším postupem můžete pokračovat pro aplikace, které využívají webové rozhraní API prostřednictvím jazyka JavaScript v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="ee01e-107">It’s all a matter of tradeoffs: and as it turns out, the implicit grant is the best approach you can pursue for applications that consume a Web API via JavaScript from a browser.</span></span>

## <a name="what-is-the-oauth2-implicit-grant"></a><span data-ttu-id="ee01e-108">Co je udělení implicitní OAuth2?</span><span class="sxs-lookup"><span data-stu-id="ee01e-108">What is the OAuth2 implicit grant?</span></span>
<span data-ttu-id="ee01e-109">Quintessential [udělení autorizačního kódu OAuth2](https://tools.ietf.org/html/rfc6749#section-1.3.1) je udělení autorizace, který používá dva samostatné koncové body.</span><span class="sxs-lookup"><span data-stu-id="ee01e-109">The quintessential [OAuth2 authorization code grant](https://tools.ietf.org/html/rfc6749#section-1.3.1) is the authorization grant which uses two separate endpoints.</span></span> <span data-ttu-id="ee01e-110">Koncový bod autorizace se používá pro fázi interakce uživatele, což vede k autorizační kód.</span><span class="sxs-lookup"><span data-stu-id="ee01e-110">The authorization endpoint is used for the user interaction phase, which results in an authorization code.</span></span> <span data-ttu-id="ee01e-111">Koncový bod token se pak použije klient pro výměnu kód pro přístupový token a často token obnovení.</span><span class="sxs-lookup"><span data-stu-id="ee01e-111">The token endpoint is then used by the client for exchanging the code for an access token, and often a refresh token as well.</span></span> <span data-ttu-id="ee01e-112">Webové aplikace je potřeba k dispozici vlastní aplikace pověření koncovému bodu tokenu, tak, aby server ověřování můžete ověřit klienta.</span><span class="sxs-lookup"><span data-stu-id="ee01e-112">Web applications are required to present their own application credentials to the token endpoint, so that the authorization server can authenticate the client.</span></span>

<span data-ttu-id="ee01e-113">[OAuth2 implicitní grant](https://tools.ietf.org/html/rfc6749#section-1.3.2) je varianta jiných uděluje autorizace.</span><span class="sxs-lookup"><span data-stu-id="ee01e-113">The [OAuth2 implicit grant](https://tools.ietf.org/html/rfc6749#section-1.3.2) is a variant of other authorization grants.</span></span> <span data-ttu-id="ee01e-114">Umožňuje klientům získat přístupový token (a požadavku id_token, při použití [OpenId Connect](http://openid.net/specs/openid-connect-core-1_0.html)) přímo z koncového bodu autorizace, bez kontaktování koncový bod token ani ověření klienta.</span><span class="sxs-lookup"><span data-stu-id="ee01e-114">It allows a client to obtain an access token (and id_token, when using [OpenId Connect](http://openid.net/specs/openid-connect-core-1_0.html)) directly from the authorization endpoint, without contacting the token endpoint nor authenticating the client.</span></span> <span data-ttu-id="ee01e-115">Tento typ variant byl speciálně pro JavaScript na základě aplikací, které běží ve webovém prohlížeči: ve specifikaci původní OAuth2 tokeny jsou vráceny ve fragmentu identifikátor URI.</span><span class="sxs-lookup"><span data-stu-id="ee01e-115">This variant was specifically designed for JavaScript based applications running in a Web browser: in the original OAuth2 specification, tokens are returned in a URI fragment.</span></span> <span data-ttu-id="ee01e-116">Který zpřístupní tokenu bits kód jazyka JavaScript v klientovi, ale zaručuje, že se nebudou zahrnuty do přesměrování směrem k serveru.</span><span class="sxs-lookup"><span data-stu-id="ee01e-116">That makes the token bits available to the JavaScript code in the client, but it guarantees they won’t be included in redirects toward the server.</span></span> <span data-ttu-id="ee01e-117">Vrácení tokeny přes prohlížeč přesměruje přímo z koncového bodu autorizace.</span><span class="sxs-lookup"><span data-stu-id="ee01e-117">Returning tokens via browser redirects directly from the authorization endpoint.</span></span> <span data-ttu-id="ee01e-118">Je také výhod odstraňuje všechny požadavky pro křížové počátek volání, které jsou nezbytné, pokud je aplikace JavaScript je nutný k obraťte se na koncový bod tokenu.</span><span class="sxs-lookup"><span data-stu-id="ee01e-118">It also has the advantage of eliminating any requirements for cross origin calls, which are necessary if the JavaScript application is required to contact the token endpoint.</span></span>

<span data-ttu-id="ee01e-119">Důležitou vlastností objektu implicitní grant OAuth2 je fakt, že například toků nikdy návratový obnovovacích tokenů klienta.</span><span class="sxs-lookup"><span data-stu-id="ee01e-119">An important characteristic of the OAuth2 implicit grant is the fact that such flows never return refresh tokens to the client.</span></span> <span data-ttu-id="ee01e-120">Jak vidíme v další části, který není nezbytně nutné a bude ve skutečnosti porušení zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="ee01e-120">As we will see in the next section, that isn’t really necessary and would in fact be a security issue.</span></span>

## <a name="suitable-scenarios-for-the-oauth2-implicit-grant"></a><span data-ttu-id="ee01e-121">Vhodný scénáře pro udělení implicitní OAuth2</span><span class="sxs-lookup"><span data-stu-id="ee01e-121">Suitable scenarios for the OAuth2 implicit grant</span></span>
<span data-ttu-id="ee01e-122">Podle specifikace OAuth2, samotné deklaruje, má implicitní grant připravit umožnit aplikacím uživatelského agenta – to znamená aplikací JavaScript spouštění v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="ee01e-122">As the OAuth2 specification itself declares, the implicit grant has been devised to enable user-agent applications – that is to say, JavaScript applications executing within a browser.</span></span> <span data-ttu-id="ee01e-123">Definující typické pro takové aplikace je, že kód jazyka JavaScript se používá pro přístup k prostředkům serveru (obvykle Web API) a pro aktualizaci aplikace UX odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="ee01e-123">The defining characteristic of such applications is that JavaScript code is used for accessing server resources (typically a Web API) and for updating the application UX accordingly.</span></span> <span data-ttu-id="ee01e-124">Představte si, že aplikace, jako je z Gmailu nebo Outlook Web Access: Když vyberete zprávy z vaší doručené poště, jenom na panelu zpráv vizualizace změny zobrazíte nové výběr, zatímco zbytek stránky zůstává beze změny.</span><span class="sxs-lookup"><span data-stu-id="ee01e-124">Think of applications like Gmail or Outlook Web Access: when you select a message from your inbox, only the message visualization panel changes to display the new selection, while the rest of the page remains unmodified.</span></span> <span data-ttu-id="ee01e-125">To je rozdíl od tradičních na základě přesměrování webové aplikace, kde každý interakci s uživatelem výsledkem zpětné volání celou stránku a vykreslování celou stránku odpovědi na nový server.</span><span class="sxs-lookup"><span data-stu-id="ee01e-125">This is in contrast with traditional redirect-based Web apps, where every user interaction results in a full page postback and a full page rendering of the new server response.</span></span>

<span data-ttu-id="ee01e-126">Aplikace, které potřebují přístup založené na jazyce JavaScript na jeho extreme se nazývají jednostránkové aplikace nebo SPA: cílem je, že tyto aplikace jenom sloužit úvodní stránce HTML a přidružené JavaScript, s všechny následné interakce se řídí webového rozhraní API volání provést prostřednictvím jazyka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="ee01e-126">Applications that take the JavaScript based approach to its extreme are called Single Page Applications, or SPAs: the idea is that those applications only serve an initial HTML page and associated JavaScript, with all subsequent interactions being driven by Web API calls performed via JavaScript.</span></span> <span data-ttu-id="ee01e-127">Ale hybridní přístupy, kdy aplikace je ve většině případů řízené zpětné volání, ale provádí volání příležitostně JS, nejsou – diskuzi o využití implicitní tok je relevantní pro ty také.</span><span class="sxs-lookup"><span data-stu-id="ee01e-127">However, hybrid approaches, where the application is mostly postback-driven but performs occasional JS calls, are not uncommon – the discussion about implicit flow usage is relevant for those as well.</span></span>

<span data-ttu-id="ee01e-128">Aplikace založené na přesměrování obvykle zabezpečit jejich požadavky pomocí souborů cookie, ale které přístup nefunguje také pro aplikace JavaScript.</span><span class="sxs-lookup"><span data-stu-id="ee01e-128">Redirect-based applications typically secure their requests via cookies, however, that approach does not work as well for JavaScript applications.</span></span> <span data-ttu-id="ee01e-129">Soubory cookie fungovat pouze v doméně, které budou byly vygenerovány pro, při volání JavaScriptu může být zaměřený na jiných doménách.</span><span class="sxs-lookup"><span data-stu-id="ee01e-129">Cookies only work against the domain they have been generated for, while JavaScript calls might be directed toward other domains.</span></span> <span data-ttu-id="ee01e-130">Ve skutečnosti, které často bude v případě: Představte si, že aplikace volání rozhraní API Microsoft Graph API, rozhraní API Office, Azure – všechny umístěný mimo doménu z kde zpracovat aplikace.</span><span class="sxs-lookup"><span data-stu-id="ee01e-130">In fact, that will frequently be the case: think of applications invoking Microsoft Graph API, Office API, Azure API – all residing outside the domain from where the application is served.</span></span> <span data-ttu-id="ee01e-131">Rostoucí trend pro aplikace JavaScript je, aby žádné back-end, předávající 100 % na 3. stran webovým rozhraním API implementovat funkce své firmy.</span><span class="sxs-lookup"><span data-stu-id="ee01e-131">A growing trend for JavaScript applications is to have no backend at all, relying 100% on 3rd party Web APIs to implement their business function.</span></span>

<span data-ttu-id="ee01e-132">V současné době je preferovanou metodu ochrany volání webového rozhraní API pomocí OAuth2 nosiče tokenu přístupu, kde každé volání je přiložena přístupový token OAuth2.</span><span class="sxs-lookup"><span data-stu-id="ee01e-132">Currently, the preferred method of protecting calls to a Web API is to use the OAuth2 bearer token approach, where every call is accompanied by an OAuth2 access token.</span></span> <span data-ttu-id="ee01e-133">Rozhraní Web API prozkoumá příchozí přístupový token, a pokud najde v ní nezbytné obory, udělí přístup k požadované operace.</span><span class="sxs-lookup"><span data-stu-id="ee01e-133">The Web API examines the incoming access token and, if it finds in it the necessary scopes, it grants access to the requested operation.</span></span> <span data-ttu-id="ee01e-134">Implicitní tok poskytuje vhodný mechanismus pro aplikace JavaScript získat přístupové tokeny webového rozhraní API, nabízí řadu výhod v souvislosti se soubory cookie:</span><span class="sxs-lookup"><span data-stu-id="ee01e-134">The implicit flow provides a convenient mechanism for JavaScript applications to obtain access tokens for a Web API, offering numerous advantages in respect to cookies:</span></span>

* <span data-ttu-id="ee01e-135">Tokeny spolehlivě získáte bez nutnosti křížové počátek volání – povinné registrace přesměrování, které se identifikátor URI, ke kterému jsou tokeny návratový zaručuje, že nejsou posunout tokeny</span><span class="sxs-lookup"><span data-stu-id="ee01e-135">Tokens can be reliably obtained without any need for cross origin calls – mandatory registration of the redirect URI to which tokens are return guarantees that tokens are not displaced</span></span>
* <span data-ttu-id="ee01e-136">JavaScript aplikací můžete získat podle potřeby, pro tolik webové rozhraní API se cíle – bez omezení v doménách přístupové tokeny</span><span class="sxs-lookup"><span data-stu-id="ee01e-136">JavaScript applications can obtain as many access tokens as they need, for as many Web APIs they target – with no restriction on domains</span></span>
* <span data-ttu-id="ee01e-137">Funkce jazyka HTML5, jako jsou relace nebo místní úložiště poskytují plnou kontrolu nad ukládání tokenu do mezipaměti a správu životního cyklu, zatímco Správa souborů cookie je neprůhledné do aplikace</span><span class="sxs-lookup"><span data-stu-id="ee01e-137">HTML5 features like session or local storage grant full control over token caching and lifetime management, whereas cookies management is opaque to the app</span></span>
* <span data-ttu-id="ee01e-138">Přístupové tokeny nejsou náchylné k útokům padělání (proti útokům CSRF) požadavek webů</span><span class="sxs-lookup"><span data-stu-id="ee01e-138">Access tokens aren’t susceptible to Cross-site request forgery (CSRF) attacks</span></span>

<span data-ttu-id="ee01e-139">Implicitní grant toku nevydává tokeny obnovení, většinou z bezpečnostních důvodů.</span><span class="sxs-lookup"><span data-stu-id="ee01e-139">The implicit grant flow does not issue refresh tokens, mostly for security reasons.</span></span> <span data-ttu-id="ee01e-140">Token obnovení není v jako přístupových tokenů, udělení podstatně více power proto inflicting podstatně více poškození v případě, že je neuniknou jako úzce oboru.</span><span class="sxs-lookup"><span data-stu-id="ee01e-140">A refresh token isn’t as narrowly scoped as access tokens, granting far more power hence inflicting far more damage in case it is leaked out.</span></span> <span data-ttu-id="ee01e-141">V implicitní tok tokeny se dodávají v adrese URL, proto je vyšší než v udělení autorizačního kódu riziko zachycení.</span><span class="sxs-lookup"><span data-stu-id="ee01e-141">In the implicit flow, tokens are delivered in the URL, hence the risk of interception is higher than in the authorization code grant.</span></span>

<span data-ttu-id="ee01e-142">Uvědomte si však, že aplikace JavaScript má jiný mechanismus k dispozici pro obnovení přístupových tokenů bez opakovaného výzvy pro uživatele pro přihlašovací údaje.</span><span class="sxs-lookup"><span data-stu-id="ee01e-142">However, note that a JavaScript application has another mechanism at its disposal for renewing access tokens without repeatedly prompting the user for credentials.</span></span> <span data-ttu-id="ee01e-143">Aplikace můžete použít skrytá iframe provést nové žádosti o tokeny na koncový bod autorizace služby Azure AD: tak dlouho, dokud prohlížeče má stále aktivní relace (čtení: obsahuje soubor cookie relace) vůči doméně služby Azure AD, můžete žádost o ověření úspěšně dojít bez nutnosti zásahu uživatele.</span><span class="sxs-lookup"><span data-stu-id="ee01e-143">The application can use a hidden iframe to perform new token requests against the authorization endpoint of Azure AD: as long as the browser still has an active session (read: has a session cookie) against the Azure AD domain, the authentication request can successfully occur without any need for user interaction.</span></span>

<span data-ttu-id="ee01e-144">Tento model umožňuje aplikace JavaScript nezávisle obnovit přístupové tokeny a i získat nové pro nové rozhraní API (za předpokladu, že uživatel dříve dá souhlas pro ně.</span><span class="sxs-lookup"><span data-stu-id="ee01e-144">This model grants the JavaScript application the ability to independently renew access tokens and even acquire new ones for a new API (provided that the user previously consented for them.</span></span> <span data-ttu-id="ee01e-145">Tím je zabráněno přidané zatížení získávání, Správa a ochrana artefaktů vysoké hodnoty například token obnovení.</span><span class="sxs-lookup"><span data-stu-id="ee01e-145">This avoids the added burden of acquiring, maintaining, and protecting a high value artifact such as a refresh token.</span></span> <span data-ttu-id="ee01e-146">Artefaktů, která umožňuje tichou obnovení souboru cookie relace Azure AD, se spravuje mimo aplikaci.</span><span class="sxs-lookup"><span data-stu-id="ee01e-146">The artifact which makes the silent renewal possible, the Azure AD session cookie, is managed outside of the application.</span></span> <span data-ttu-id="ee01e-147">Další výhodou tohoto přístupu je, že uživatel může odhlásit se ze služby Azure AD pomocí některé z aplikací přihlášení k Azure AD, spuštěná v jakémkoli záložkách prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="ee01e-147">Another advantage of this approach is a user can sign out from Azure AD, using any of the applications signed into Azure AD, running in any of the browser tabs.</span></span> <span data-ttu-id="ee01e-148">Výsledkem je odstranění souboru cookie relace Azure AD a aplikace JavaScript automaticky ztratíte možnost obnovit tokeny pro podepsané na uživatele.</span><span class="sxs-lookup"><span data-stu-id="ee01e-148">This results in the deletion of the Azure AD session cookie, and the JavaScript application will automatically lose the ability to renew tokens for the signed out user.</span></span>

## <a name="is-the-implicit-grant-suitable-for-my-app"></a><span data-ttu-id="ee01e-149">Implicitní grant je vhodný pro mé aplikace?</span><span class="sxs-lookup"><span data-stu-id="ee01e-149">Is the implicit grant suitable for my app?</span></span>
<span data-ttu-id="ee01e-150">Implicitní grant představuje další rizik než ostatní uděluje a oblastí, je potřeba věnovat pozornost jsou popsány.</span><span class="sxs-lookup"><span data-stu-id="ee01e-150">The implicit grant presents more risks than other grants, and the areas you need to pay attention to are well documented.</span></span> <span data-ttu-id="ee01e-151">Například [zneužití přístup Token zosobnění vlastníka prostředku v implicitní tok] [ OAuth2-Spec-Implicit-Misuse] a [Model hrozeb OAuth 2.0 a aspekty zabezpečení] [ OAuth2-Threat-Model-And-Security-Implications]).</span><span class="sxs-lookup"><span data-stu-id="ee01e-151">For example, [Misuse of Access Token to Impersonate Resource Owner in Implicit Flow][OAuth2-Spec-Implicit-Misuse] and [OAuth 2.0 Threat Model and Security Considerations][OAuth2-Threat-Model-And-Security-Implications]).</span></span> <span data-ttu-id="ee01e-152">Však vyšší riziko profilu je z velké části vzhledem k tomu, že smyslem je aplikace, které spustit kód s aktivní, obsloužených vzdálený prostředek do prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="ee01e-152">However, the higher risk profile is largely due to the fact that it is meant to enable applications that execute active code, served by a remote resource to a browser.</span></span> <span data-ttu-id="ee01e-153">Pokud plánujete SPA architekturu, jste žádné součásti back-end nebo volání webového rozhraní API prostřednictvím jazyka JavaScript v úmyslu, se doporučuje použít implicitní tok pro získání tokenu.</span><span class="sxs-lookup"><span data-stu-id="ee01e-153">If you are planning an SPA architecture, have no backend components or intend to invoke a Web API via JavaScript, use of the implicit flow for token acquisition is recommended.</span></span>

<span data-ttu-id="ee01e-154">Pokud vaše aplikace je nativní klient, není implicitní tok skvělé přizpůsobit.</span><span class="sxs-lookup"><span data-stu-id="ee01e-154">If your application is a native client, the implicit flow isn’t a great fit.</span></span> <span data-ttu-id="ee01e-155">Chybí soubor cookie relace Azure AD v kontextu nativního klienta zbaví aplikace znamená udržovat relaci dlouho povahy.</span><span class="sxs-lookup"><span data-stu-id="ee01e-155">The absence of the Azure AD session cookie in the context of a native client deprives your application from the means of maintaining a long lived session.</span></span> <span data-ttu-id="ee01e-156">To znamená, vaše aplikace vyzve opakovaně uživatele při získání přístupových tokenů pro nové prostředky.</span><span class="sxs-lookup"><span data-stu-id="ee01e-156">Which means your application will repeatedly prompt the user when obtaining access tokens for new resources.</span></span>

<span data-ttu-id="ee01e-157">Pokud vyvíjíte webovou aplikaci, která zahrnuje back-end a využívání rozhraní API z jeho back-end kód, implicitní tok je také vhodné.</span><span class="sxs-lookup"><span data-stu-id="ee01e-157">If you are developing a Web application which includes a backend, and consuming an API from its backend code, the implicit flow is also not a good fit.</span></span> <span data-ttu-id="ee01e-158">Další uděluje získáte mnohem více energie.</span><span class="sxs-lookup"><span data-stu-id="ee01e-158">Other grants give you far more power.</span></span> <span data-ttu-id="ee01e-159">Například udělení pověření klienta OAuth2 poskytuje schopnost získat tokeny, které odráží oprávnění přiřazená samotné, aplikace a delegování uživatele.</span><span class="sxs-lookup"><span data-stu-id="ee01e-159">For example, the OAuth2 client credentials grant provides the ability to obtain tokens that reflect the permissions assigned to the application itself, as opposed to user delegations.</span></span> <span data-ttu-id="ee01e-160">To znamená, že klient má možnost spravovat i programový přístup k prostředkům, pokud uživatel není aktivně zapojený do relace a tak dále.</span><span class="sxs-lookup"><span data-stu-id="ee01e-160">This means the client has the ability to maintain programmatic access to resources even when a user is not actively engaged in a session, and so on.</span></span> <span data-ttu-id="ee01e-161">Nejenom, ale tyto uděluje poskytují vyšší záruky zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="ee01e-161">Not only that, but such grants give higher security guarantees.</span></span> <span data-ttu-id="ee01e-162">Například přístupové tokeny nikdy přenosu prostřednictvím prohlížeče uživatele, nemají riziko uloženy v historii prohlížeče a tak dále.</span><span class="sxs-lookup"><span data-stu-id="ee01e-162">For instance, access tokens never transit through the user browser, they don’t risk being saved in the browser history, and so on.</span></span> <span data-ttu-id="ee01e-163">Klientská aplikace můžete také provést silné ověřování, žádosti o token.</span><span class="sxs-lookup"><span data-stu-id="ee01e-163">The client application can also perform strong authentication when requesting a token.</span></span>

## <a name="next-steps"></a><span data-ttu-id="ee01e-164">Další kroky</span><span class="sxs-lookup"><span data-stu-id="ee01e-164">Next steps</span></span>
* <span data-ttu-id="ee01e-165">Úplný seznam materiály pro vývojáře, včetně referenční informace pro protokoly a ověřování OAuth2 udělit toky podpory Azure AD, najdete v části [Příručka vývojáře pro Azure AD][AAD-Developers-Guide]</span><span class="sxs-lookup"><span data-stu-id="ee01e-165">For a complete list of developer resources, including reference information for the protocols and OAuth2 authorization grant flows support by Azure AD, refer to the [Azure AD Developer's Guide][AAD-Developers-Guide]</span></span>
* <span data-ttu-id="ee01e-166">V tématu [postup při integraci aplikace s Azure AD] [ ACOM-How-To-Integrate] pro další hloubku na proces integrace aplikací.</span><span class="sxs-lookup"><span data-stu-id="ee01e-166">See [How to integrate an application with Azure AD][ACOM-How-To-Integrate] for additional depth on the application integration process.</span></span>

<!--Image references-->

<!--Reference style links in use-->
[AAD-Developers-Guide]: active-directory-developers-guide.md
[ACOM-How-And-Why-Apps-Added-To-AAD]: active-directory-how-applications-are-added.md
[ACOM-How-To-Integrate]: active-directory-how-to-integrate.md
[OAuth2-Spec-Implicit-Misuse]: https://tools.ietf.org/html/rfc6749#section-10.16
[OAuth2-Threat-Model-And-Security-Implications]: https://tools.ietf.org/html/rfc6819
