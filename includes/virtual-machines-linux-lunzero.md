<span data-ttu-id="9e756-101">Při přidávání dat tooa disky virtuálního počítače s Linuxem, mohou nastat chyby, pokud disk neexistuje na logické jednotce 0.</span><span class="sxs-lookup"><span data-stu-id="9e756-101">When adding data disks tooa Linux VM, you may encounter errors if a disk does not exist at LUN 0.</span></span> <span data-ttu-id="9e756-102">Pokud přidáváte ručně pomocí hello disk `azure vm disk attach-new` příkazu a zadáte na logické jednotce (`--lun`) namísto povolení hello platformy Azure toodetermine hello odpovídající logické jednotky, ujistěte se, že disk již existuje / bude existovat na logické jednotce 0.</span><span class="sxs-lookup"><span data-stu-id="9e756-102">If you are adding a disk manually using hello `azure vm disk attach-new` command and you specify a LUN (`--lun`) rather than allowing hello Azure platform toodetermine hello appropriate LUN, take care that a disk already exists / will exist at LUN 0.</span></span> 

<span data-ttu-id="9e756-103">Vezměte v úvahu následující příklad zobrazuje fragment výstup hello hello `lsscsi`:</span><span class="sxs-lookup"><span data-stu-id="9e756-103">Consider hello following example showing a snippet of hello output from `lsscsi`:</span></span>

```bash
[5:0:0:0]    disk    Msft     Virtual Disk     1.0   /dev/sdc 
[5:0:0:1]    disk    Msft     Virtual Disk     1.0   /dev/sdd 
```

<span data-ttu-id="9e756-104">Hello dvě datové disky existovat logická jednotka 0 a 1 logická jednotka (hello první sloupec v hello `lsscsi` výstup podrobnosti `[host:channel:target:lun]`).</span><span class="sxs-lookup"><span data-stu-id="9e756-104">hello two data disks exist at LUN 0 and LUN 1 (hello first column in hello `lsscsi` output details `[host:channel:target:lun]`).</span></span> <span data-ttu-id="9e756-105">Oba disky by měl být accessbile z v rámci hello virtuálních počítačů.</span><span class="sxs-lookup"><span data-stu-id="9e756-105">Both disks should be accessbile from within hello VM.</span></span> <span data-ttu-id="9e756-106">Pokud byl ručně zadané hello první disk toobe přidat na logickou jednotku LUN 1 a hello druhý disk na logické jednotce 2, nemusíte vidět hello disky správně ze v rámci virtuálního počítače.</span><span class="sxs-lookup"><span data-stu-id="9e756-106">If you had manually specified hello first disk toobe added at LUN 1 and hello second disk at LUN 2, you may not see hello disks correctly from within your VM.</span></span>

> [!NOTE]
> <span data-ttu-id="9e756-107">Hello Azure `host` hodnota je 5 v těchto příkladech, ale to se může lišit v závislosti na typu hello úložiště, které vyberete.</span><span class="sxs-lookup"><span data-stu-id="9e756-107">hello Azure `host` value is 5 in these examples, but this may vary depending on hello type of storage you select.</span></span>
> 
> 

<span data-ttu-id="9e756-108">Toto chování disku není problém s Azure, ale způsob hello, ve které hello Linux jádra následuje specifikace SCSI hello.</span><span class="sxs-lookup"><span data-stu-id="9e756-108">This disk behavior is not an Azure problem, but hello way in which hello Linux kernel follows hello SCSI specifications.</span></span> <span data-ttu-id="9e756-109">Sběrnice SCSI hello hledá připojená zařízení prohledávání hello Linux jádra zařízení musí být nalezen na logickou jednotku LUN 0 v pořadí pro toocontinue systému hello vyhledávání dalších zařízení.</span><span class="sxs-lookup"><span data-stu-id="9e756-109">When hello Linux kernel scans hello SCSI bus for attached devices, a device must be found at LUN 0 in order for hello system toocontinue scanning for additional devices.</span></span> <span data-ttu-id="9e756-110">Takto:</span><span class="sxs-lookup"><span data-stu-id="9e756-110">As such:</span></span>

* <span data-ttu-id="9e756-111">Zkontrolujte výstup hello `lsscsi` po přidání tooverify disku data máte disk na logické jednotce 0.</span><span class="sxs-lookup"><span data-stu-id="9e756-111">Review hello output of `lsscsi` after adding a data disk tooverify that you have a disk at LUN 0.</span></span>
* <span data-ttu-id="9e756-112">Pokud disk nezobrazuje správně v rámci virtuálního počítače, ověřte, zda že disk na logické jednotce 0 existuje.</span><span class="sxs-lookup"><span data-stu-id="9e756-112">If your disk does not show up correctly within your VM, verify a disk exists at LUN 0.</span></span>

